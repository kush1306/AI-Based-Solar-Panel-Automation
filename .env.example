# Copy this file to .env (at repo root) and adjust values as needed
# .env should be added to .gitignore — never commit real credentials

MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=solar_demand_db
MYSQL_USER=admin
MYSQL_PASSWORD=admin

# Used by backend (FastAPI/SQLAlchemy) to connect to MySQL
# Format: mysql+pymysql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=mysql+pymysql://admin:admin@db:3306/solar_demand_db

# Backend individual connection vars (local development)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=solar_panel_automation
DB_USER=your_mysql_user
DB_PASSWORD=your_mysql_password
